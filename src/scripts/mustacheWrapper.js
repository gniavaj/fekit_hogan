function (c, p, i) {
    var _ = this;
    _.b(i = i || "");

    if (_.s(
        _.f("name", c, p, 1), c, p, 0, 9, 13, "{{ }}"
    )) {
        _.rs(
            c, p, function (c, p, _) {
                _.b("mike");
            }
        );
        c.pop();
    }
    return _.fl();
    ;
}